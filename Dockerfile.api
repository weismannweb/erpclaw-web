FROM python:3.11-slim
WORKDIR /app
COPY api/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt uvicorn
COPY api ./api
COPY core ./core
ENV ERPCLAW_ENV=production
EXPOSE 8100
CMD ["uvicorn", "api.main:app", "--host", "0.0.0.0", "--port", "8100"]
